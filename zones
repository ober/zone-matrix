;; This buffer is for notes you don't want to save, and for Lisp evaluation.
;; If you want to create a file, visit that file with C-x C-f,
;; then enter the text in that file's own buffer.

(setq zone-programs [zone-pgm-rotate])
(setq zone-programs [zone-pgm-explode])
rotate
(setq zone-programs [zone-pgm-jitter]) (zone)
(setq zone-programs [zone-pgm-putz-with-case]) (zone)
(setq zone-programs [zone-pgm-dissolve]) (zone)
(setq zone-programs [zone-pgm-explode]) (zone)
(setq zone-programs [zone-pgm-whack-chars]) (zone)
(setq zone-programs [zone-pgm-rotate]) (zone)
(setq zone-programs [zone-pgm-rotate-LR-lockstep]) (zone)
(setq zone-programs [zone-pgm-rotate-RL-lockstep]) (zone)
(setq zone-programs [zone-pgm-rotate-LR-variable]) (zone)
(setq zone-programs [zone-pgm-rotate-RL-variable]) (zone)
(setq zone-programs [zone-pgm-drip]) (zone)
(setq zone-programs [zone-pgm-drip-fretfully]) (zone)
(setq zone-programs [zone-pgm-five-oclock-swan-dive]) (zone)
(setq zone-programs [zone-pgm-martini-swan-dive]) (zone)
(setq zone-programs [zone-pgm-rat-race]) (zone)
(setq zone-programs [zone-pgm-paragraph-spaz]) (zone)
(setq zone-programs [zone-pgm-stress]) (zone)
(setq zone-programs [zone-pgm-stress-destress]) (zone)
(setq zone-programs [zone-pgm-random-life]) (zone)
(setq zone-programs [zone-pgm-random-life]) (zone)
